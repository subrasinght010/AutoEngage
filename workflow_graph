---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__(<p>__start__</p>)
	db(db)
	data_entry_node(data_entry_node)
	scheduler_agent(scheduler_agent)
	inbound_call_node(inbound_call_node)
	inbound_mail_node(inbound_mail_node)
	inbound_whatsapp_node(inbound_whatsapp_node)
	db_analyser(db_analyser)
	verify_data_node(verify_data_node)
	intent_detector_node(intent_detector_node)
	calling_agent_node(calling_agent_node)
	follow_up_node(follow_up_node)
	db_update_node(db_update_node)
	mail_agent(mail_agent)
	whatapp_agent(whatapp_agent)
	__start__ --> data_entry_node;
	calling_agent_node --> db_update_node;
	data_entry_node --> db;
	db --> db_analyser;
	db --> mail_agent;
	db --> whatapp_agent;
	db_analyser --> intent_detector_node;
	db_update_node --> db;
	intent_detector_node --> scheduler_agent;
	mail_agent --> db_update_node;
	scheduler_agent --> verify_data_node;
	verify_data_node --> calling_agent_node;
	verify_data_node --> mail_agent;
	verify_data_node --> whatapp_agent;
	whatapp_agent --> db_update_node;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
