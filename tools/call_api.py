# Placeholder call API (e.g., Twilio). Replace with a real implementation.
class CallAPI:
    def call(self, phone: str, message: str):
        print(f"[CALL] Calling {phone} with message: {message}")
